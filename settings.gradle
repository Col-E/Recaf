rootProject.name = 'recaf'
include 'recaf-utils',
        'recaf-core',
        'recaf-launcher',
        'recaf-ui',
        'recaf-plugin',
        'recaf-assembler'

if (!JavaVersion.current().isJava9Compatible()) {
    include 'recaf-facade'
}
